# clients/3ds

Nintendo 3DS Homebrew-Client (devkitARM / libctru). Bindet
[`../../core/`](../../core/) ein, implementiert Netzwerk, Rendering, Audio-Ausgabe
und Input-Polling mit libctru-APIs.

Noch nicht implementiert.
