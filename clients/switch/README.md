# clients/switch

Nintendo Switch Homebrew-Client (devkitA64 / libnx). Bindet
[`../../core/`](../../core/) ein, implementiert Netzwerk, Rendering, Audio-Ausgabe
und Input-Polling mit libnx-APIs.

Noch nicht implementiert.
